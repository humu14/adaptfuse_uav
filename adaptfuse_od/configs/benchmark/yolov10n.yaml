# YOLOv10 Nano — NMS-free design
experiment_name: "benchmark_yolov10n"

model:
  architecture: "yolov10n"
  pretrained: true
  num_classes: 2

dataset:
  name: "dfire"
  image_size: 640

training:
  epochs: 100
  batch_size: 16
  lr: 0.001
  early_stopping_patience: 20
