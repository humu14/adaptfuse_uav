# AdapFuse-UAV demo app (live + Gradio). Tested with Python 3.12, torch 2.2.2+cu121.
# Install PyTorch first for your CUDA version (https://pytorch.org/get-started/locally/),
# e.g.  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16
ultralytics>=8.4          # YOLO26 support
numpy>=1.24,<2.3
opencv-python>=4.8
librosa>=0.10
imageio-ffmpeg>=0.5       # bundled ffmpeg for audio extraction / H.264 encoding
matplotlib>=3.7
fastapi>=0.110
uvicorn>=0.29
websockets>=12
python-multipart>=0.0.9
gradio>=5.0
